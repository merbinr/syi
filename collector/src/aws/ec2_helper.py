import boto3

from utils.logger import get_logger

logger = get_logger()


def get_lb_data(session: boto3.Session) -> list[dict]:
    pass


def get_ec2_ip_data(session: boto3.Session) -> list[dict]:
    pass
